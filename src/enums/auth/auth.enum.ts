export enum Role {
    ADMIN = "admin",
    NORMAL_USER = "normalUser",
    PREMIUM_USER = "premiumUser",
}

export enum Action {
    MANAGE = "manage",
    CREATE = "create",
    READ = "read",
    UPDATE = "update",
    DELETE = "delete",
}
