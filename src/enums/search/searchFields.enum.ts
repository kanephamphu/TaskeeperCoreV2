export enum SearchTags {
    USING_TIMES = "usingTimes",
}

export enum SearchUsers {
    ID = "_id",
    FIRST_NAME = "firstName",
    LAST_NAME = "lastName",
    CREATED_AT = "createdAt",
}

export enum SearchJobs {
    ID = "_id",
    DESCRIPTION = "description",
    TITLE = "title",
    JOB_TYPE = "jobType",
    CREATED_AT = "createdAt",
}
